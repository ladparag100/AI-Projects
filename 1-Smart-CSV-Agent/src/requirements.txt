# Smart CSV Agent - Streamlit app dependencies
# Note: langchain-google-genai requires the LangChain 1.x line, so these
# are left unpinned (unlike older projects in this repo) to let pip
# resolve a mutually compatible set.

langchain-experimental
langchain-google-genai

pandas>=2.0.0
numpy>=1.26.0
tabulate>=0.9.0

streamlit>=1.28.0
