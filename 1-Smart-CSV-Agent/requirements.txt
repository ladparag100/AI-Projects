# Smart CSV Agent Dependencies
# Note: langchain-google-genai requires the LangChain 1.x line, so these
# are left unpinned to let pip resolve a mutually compatible set.

# Core AI/ML Libraries
langchain-experimental
langchain-google-genai

# Data Processing
pandas>=2.0.0
numpy>=1.26.0
tabulate>=0.9.0

# Web Framework
streamlit>=1.28.0

# Utilities
requests>=2.31.0
gdown>=4.7.1

# Jupyter
jupyter>=1.0.0
ipython>=8.0.0
